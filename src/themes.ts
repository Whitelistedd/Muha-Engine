export const theme: themeType = {
  light: {
    background: '#FFFFFF',
    TextColor: '#000000',
  },
  dark: {
    background: '#FFFFFF',
    TextColor: '#000000',
  },
}

export type themeType = {
  light: {
    background: string
    TextColor: string
  }
  dark: {
    background: string
    TextColor: string
  }
}
